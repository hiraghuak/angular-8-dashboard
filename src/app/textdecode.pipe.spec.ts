import { TextdecodePipe } from './textdecode.pipe';

describe('TextdecodePipe', () => {
  it('create an instance', () => {
    const pipe = new TextdecodePipe();
    expect(pipe).toBeTruthy();
  });
});
